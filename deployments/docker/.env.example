# x-extract Docker Environment Variables
# Copy this file to .env and modify as needed

# Port configuration
EXPOSE_PORT=9090

# Config directory (stores config.yaml and queue.db)
CONFIG_DIR=./config

# Downloads directory (stores downloaded files, cookies, logs)
DOWNLOADS_DIR=./downloads
